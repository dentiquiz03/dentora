import { NextResponse } from "next/server";import { questionSchema } from "@/lib/question-schema";import { getAdminApiContext } from "@/lib/api-auth";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const {supabase,user,isAdmin}=await getAdminApiContext();if(!user||!isAdmin)return NextResponse.json({error:"Administrator access required"},{status:403});const input=questionSchema.parse(await request.json());const {options,...question}=input;const {error}=await supabase.from("questions").update({...question,updated_by:user.id}).eq("id",id);if(error)throw error;const {error:delError}=await supabase.from("question_options").delete().eq("question_id",id);if(delError)throw delError;const {error:optError}=await supabase.from("question_options").insert(options.map((o,i)=>({...o,question_id:id,sort_order:i})));if(optError)throw optError;return NextResponse.json({id});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Could not update question"},{status:400});}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,isAdmin}=await getAdminApiContext();if(!isAdmin)return NextResponse.json({error:"Administrator access required"},{status:403});const {error}=await supabase.from("questions").delete().eq("id",id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
