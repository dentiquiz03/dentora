import { PageHeading } from "@/components/page-heading";import { TaxonomyManager } from "@/components/taxonomy-manager";import { requireAdmin } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";import type {Subject,Topic} from "@/lib/types";export const metadata={title:"Subjects and topics"};export default async function TaxonomyPage(){await requireAdmin();const s=await createClient();const [{data:subjects},{data:topics}]=await Promise.all([s.from("subjects").select("id,name,slug,icon").order("sort_order"),s.from("topics").select("id,subject_id,name,slug").order("sort_order")]);return <><PageHeading eyebrow="Classification" title="Subjects and topics" description="Keep question filters organised with a clear dental curriculum structure."/><TaxonomyManager subjects={(subjects||[]) as Subject[]} topics={(topics||[]) as Topic[]}/></>}
