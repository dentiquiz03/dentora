import { PageHeading } from "@/components/page-heading";import { QuestionForm } from "@/components/question-form";import { requireAdmin } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";import type {Subject,Topic} from "@/lib/types";
export const metadata={title:"New question"};export default async function NewQuestionPage(){await requireAdmin();const s=await createClient();const [{data:subjects},{data:topics}]=await Promise.all([s.from("subjects").select("id,name,slug,icon").order("sort_order"),s.from("topics").select("id,subject_id,name,slug").order("sort_order")]);return <><PageHeading eyebrow="Question editor" title="Create a question" description="Build a complete question with classification, answer key and teaching explanation."/><QuestionForm subjects={(subjects||[]) as Subject[]} topics={(topics||[]) as Topic[]}/></>}
