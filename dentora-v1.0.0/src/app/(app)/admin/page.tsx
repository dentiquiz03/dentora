import Link from "next/link";
import { FileQuestion, Import, Library, Send, Users } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { StatCard } from "@/components/stat-card";
import { requireAdmin } from "@/lib/auth";
import { createClient } from "@/lib/supabase/server";
export const metadata={title:"Administration"};
export default async function AdminPage(){await requireAdmin();const supabase=await createClient();const [{count:questions},{count:published},{count:users},{count:attempts}]=await Promise.all([supabase.from("questions").select("*",{count:"exact",head:true}),supabase.from("questions").select("*",{count:"exact",head:true}).eq("status","published"),supabase.from("profiles").select("*",{count:"exact",head:true}),supabase.from("attempts").select("*",{count:"exact",head:true})]);return <><PageHeading eyebrow="Administration" title="Question bank control centre" description="Manage content, imports, taxonomy and the two private user accounts." action={<Link className="btn btn-primary" href="/admin/questions/new"><FileQuestion size={18}/>New question</Link>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="All questions" value={questions||0} icon={Library}/><StatCard label="Published" value={published||0} icon={Send}/><StatCard label="Users" value={users||0} icon={Users}/><StatCard label="Learner attempts" value={attempts||0} icon={FileQuestion}/></div><div className="mt-5 grid gap-4 md:grid-cols-3"><Link href="/admin/questions" className="card p-6 transition hover:-translate-y-1"><Library className="text-[var(--primary)]"/><h2 className="mt-6 text-xl font-black">Question library</h2><p className="mt-2 text-sm leading-6 text-slate-500">Create, edit, publish, duplicate and archive individual questions.</p></Link><Link href="/admin/import" className="card p-6 transition hover:-translate-y-1"><Import className="text-violet-600"/><h2 className="mt-6 text-xl font-black">Bulk importer</h2><p className="mt-2 text-sm leading-6 text-slate-500">Validate and upload hundreds of questions from one CSV spreadsheet.</p></Link><Link href="/admin/users" className="card p-6 transition hover:-translate-y-1"><Users className="text-orange-600"/><h2 className="mt-6 text-xl font-black">Private users</h2><p className="mt-2 text-sm leading-6 text-slate-500">Create the learner account and inspect account roles.</p></Link></div></>}
