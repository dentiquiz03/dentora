import { PageHeading } from "@/components/page-heading";import { CsvImporter } from "@/components/csv-importer";import { requireAdmin } from "@/lib/auth";export const metadata={title:"Bulk import"};export default async function ImportPage(){await requireAdmin();return <><PageHeading eyebrow="Bulk content" title="Import questions from CSV" description="Upload a spreadsheet, preview the data and receive precise row-level error reporting."/><CsvImporter/></>}
