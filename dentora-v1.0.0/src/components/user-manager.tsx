"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { KeyRound, Loader2, UserPlus } from "lucide-react";
export function UserManager(){const router=useRouter();const [email,setEmail]=useState("");const [name,setName]=useState("");const [password,setPassword]=useState("");const [loading,setLoading]=useState(false);const [message,setMessage]=useState("");async function create(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage("");const res=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,display_name:name})});const json=await res.json();if(!res.ok)setMessage(json.error||"Could not create user");else{setMessage("Learner account created successfully.");setEmail("");setName("");setPassword("");router.refresh()}setLoading(false)}return <form onSubmit={create} className="card p-5 md:p-6"><h2 className="flex items-center gap-2 text-lg font-black"><UserPlus size={20}/>Create learner account</h2><p className="mt-2 text-sm leading-6 text-slate-500">The learner can change the temporary password later through Supabase password recovery.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><label className="label">Display name</label><input className="input" required value={name} onChange={e=>setName(e.target.value)} placeholder="Friend's name"/></div><div><label className="label">Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="friend@example.com"/></div><div className="sm:col-span-2"><label className="label">Temporary password</label><div className="relative"><KeyRound className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={17}/><input className="input pl-10" type="password" minLength={10} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 10 characters"/></div></div></div>{message&&<p className={`mt-4 rounded-xl p-3 text-sm font-semibold ${message.includes("success")?"bg-emerald-50 text-emerald-700":"bg-red-50 text-red-700"}`}>{message}</p>}<button disabled={loading} className="btn btn-primary mt-5">{loading?<Loader2 className="animate-spin" size={18}/>:<UserPlus size={18}/>}Create learner</button></form>}
